'use strict';

/**
 * bossiness-partner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bossiness-partner.bossiness-partner');
